apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "metadata-db.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "metadata-db.name" . }}
    helm.sh/chart: {{ include "metadata-db.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "metadata-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
